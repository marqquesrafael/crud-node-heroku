# crud-node-heroku

npm init -y

npm i express cors knex dotenv

criar arquivo .gitignore e adcionar node_modules e dotenv

criar arquivo .env e inserir o valor da porta(PORT=3000) e dados de acesso ao banco

ajustar apontamento do main:"src/index.js" no arquivo package.json

npx knex init

configurar o arquivo knexfile.js, apagar as demais conexões e adaptar a conexão development







